# Contributors

UCSD ECE 268 Hardware Security final project — group submission.

All three members contributed equally to the GGM Tree on GPU project. The
topical responsibilities below mirror the split in
`docs/superpowers/specs/2026-05-18-ggm-tree-design.md` §12.

| Member   | Primary topic                                                       | Secondary topic           |
|----------|---------------------------------------------------------------------|---------------------------|
| Member 1 | AES PRG kernels (S-box / T-tables / bitsliced) + AES KAT            | Benchmark harness         |
| Member 2 | Spongent-$\pi[176]$ PRG kernel + Spongent KAT + CPU baselines       | Plots and figures         |
| Member 3 | GPU memory layout, parallelism kernels, CPU OpenMP integration      | Report and slides         |

Replace `Member 1 / 2 / 3` with each member's name + UCSD email before final
submission. The GitHub commit graph reflects the actual author of each commit;
if everyone has merged at least one PR from their own account, the graph
matches the topical split here.
